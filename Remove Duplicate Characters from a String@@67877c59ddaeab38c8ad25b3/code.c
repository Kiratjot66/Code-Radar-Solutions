#include <stdio.h>
itn main(){
    char a[1000];
    fgets(a,sizeof(a),stdin)
    for(int i=0;i<sizeof(a);i++){
        c=a[i];
        for(int j=i+1;j<sizeof(a);j++){
            
        }
    }
}